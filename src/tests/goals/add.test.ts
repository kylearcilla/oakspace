import { describe, it, expect, beforeEach } from 'vitest'

import { initGoals } from '$lib/utils-goals'
import { GOALS } from '$lib/mock-data-goals'
import { addGoal, getMonthData, getQuarterData, getYearData } from '$lib/utils-goals'

describe('addGoal', () => {
    beforeEach(() => initGoals())

    it('add goal mar: 2nd position in default ', () => {
    })
    it('add goal mar: 2nd position in default ', () => {
    })
})
