import { describe, it, expect, beforeEach } from 'vitest'
import { findSectionNbrs, initGoals } from '$lib/utils-goals'

describe('removal in month view', () => {
    beforeEach(() => initGoals())

    // remove from march (default)

    it('month check: default, status tag', () => {

    })

    // remove from may (tag)
    
    it('remove from march: should reflect in other group views', () => {

    })

    // remove from may (tag)
    
})

describe('removal in quarter view', () => {
    beforeEach(() => initGoals())

})

describe('removal in year view', () => {
    beforeEach(() => initGoals())

})